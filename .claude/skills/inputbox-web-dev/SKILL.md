---
name: inputbox-web-dev
description: Claude Code 對 InputBox gh-pages 網站工程技能的橋接。當修改 index.html、CSS、多語系、A11y、測試或 Git 工作流時使用。
---

# InputBox gh-pages Claude Code Skill Bridge

本檔只負責讓 Claude Code discovery 找到 InputBox `gh-pages` project skill。權威 skill 位於 `.agents/skills/inputbox-web-dev/SKILL.md`。

開始工作前：

1. 讀取 `.agents/skills/inputbox-web-dev/SKILL.md`。
2. 依 `AGENTS.md` 確認載入順序與安全紅線。
3. 讀取 `docs/engineering/` 下的任務相關規範。

不要在本檔維護第二份網站工程規則。
