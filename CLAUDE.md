# InputBox gh-pages - Claude Code 入口

本檔是 Claude Code 在 `gh-pages` 分支的專案記憶入口。Claude Code 官方文件建議在已有 `AGENTS.md` 的 repo 內，用 `@AGENTS.md` 匯入共同規範，避免重複維護。

@AGENTS.md

## Claude Code 相容要求

- 若本檔與 `AGENTS.md`、`.agents/skills/inputbox-web-dev/SKILL.md` 或 `docs/engineering/` 有衝突，以後三者為準。
- 開始任何修改前，先依 `AGENTS.md` 載入 `inputbox-web-dev` 與任務相關網頁規範。
- 不要把完整規範貼回本檔；需要 Claude 專屬補充時，只加入最小相容說明。
