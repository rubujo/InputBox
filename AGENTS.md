# InputBox - Agent 工作區入口

本檔是 InputBox repo root 的唯一跨工具 agent 指引入口，只保留載入順序、授權邊界與不可違反的安全紅線。任務路由與工程細節由 `.agents/skills/inputbox-dev/SKILL.md` 及 `docs/engineering/` 維護；工具支援資訊見 `docs/engineering/agent-support.md`。

開始任何修改前，先讀取 `.agents/skills/inputbox-dev/SKILL.md`，再依該 skill 的任務路由讀取相關工程文件。Claude Code 只透過 `CLAUDE.md` 與 `.claude/skills/inputbox-dev/SKILL.md` 橋接到同一條權威鏈，不維護第二份完整規範。

不要新增重複的 root instructions、舊版相容入口，或任何工具專屬的完整規範副本。

## 1. 授權邊界

- 回答、說明、審查、診斷或規劃型任務：檢查相關資料並回報，不要自行實作變更。
- 修改、建置或修復型任務：完成請求範圍內的本機變更與相關非破壞性驗證，不需為安全且必要的本機步驟另行停問。
- 外部寫入、破壞性操作、購買，或實質擴張使用者指定範圍前，必須先取得確認。

## 2. 安全性紅線

- 禁止記憶體注入、封包攔截、電磁紀錄，或修改第三方程式行為。
- 禁止模擬輸入到其他視窗；輸出邊界僅限於「複製到剪貼簿」。
- 禁止實作任何自動化遊戲行為，例如自動連點、自動施法、輪播或掛機控制。
- 禁止主動偵測特定第三方應用程式。
- Git 提交必須使用使用者既有的 GPG 簽章設定；不得修改 `gpg.conf`、`gpg-agent.conf`、`common.conf` 或相關簽章設定。若簽章失敗，停止並回報。

若任務涉及輸入流程、剪貼簿流程、快速鍵、控制器映射、輸出行為或返回前景視窗邏輯，交付前必須依 `docs/engineering/git-commit-safety.md` 完成即時合規檢查。

## 3. 完成條件

- 依 project skill 載入任務相關規範，完成其中要求的建置、測試、診斷、在地化或人工驗證。
- 回報實際執行的驗證與結果；必要檢查失敗或無法執行時，不得宣稱工作已完整驗證。
- 保留既有功能與使用者可見行為，除非任務明確要求改變；不要以刪除或停用必要行為換取檢查通過。
